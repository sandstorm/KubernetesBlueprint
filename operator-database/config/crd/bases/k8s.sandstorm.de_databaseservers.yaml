---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: databaseservers.k8s.sandstorm.de
spec:
  group: k8s.sandstorm.de
  names:
    kind: DatabaseServer
    listKind: DatabaseServerList
    plural: databaseservers
    singular: databaseserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatabaseServer is the Schema for the databaseservers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of DatabaseServer
            type: object
            required:
              - type
              - host
              - adminUser
              - adminPasswordSecret
            properties:
              type:
                type: string
                description: "Database server type: postgres, mariadb, or clickhouse"
                enum:
                  - postgres
                  - mariadb
                  - clickhouse
              host:
                type: string
                description: "Hostname of the database server"
              port:
                type: integer
                description: "Port of the database server (e.g. 5432 for postgres, 3306 for mariadb)"
              adminUser:
                type: string
                description: "Admin username for the database server"
              adminPasswordSecret:
                type: string
                description: "Name of a Secret (in the operator namespace) containing key 'admin_password'"
          status:
            description: Status defines the observed state of DatabaseServer
            type: object
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
